# Shared dependencies for every environment. Contains no CUDA wheels and no
# torch pin: the torch build is chosen by the environment file that includes
# this one (requirements/mac.txt or requirements/gpu.txt), because that is the
# only pin that differs between Apple Silicon and rented GPU hardware.
#
# Library versions are held at their January-2025 era to match the model cutoff
# declared in docs/spec.md (2025-01-31).

# Data
polars==1.16.0
pandas==2.2.3
pyarrow==18.1.0
numpy==2.1.3
PyYAML==6.0.2

# Modelling
transformers==4.46.3
tokenizers==0.20.3
sentence-transformers==3.3.1
scikit-learn==1.5.2
scipy==1.14.1

# Translation. Read by `scripts/translate_labels.py` alone, which writes
# reference/label_translations.json once; the pipeline looks translations up and
# never loads a translation model, so no run imports this.
sentencepiece==0.2.0

# Parameter-efficient fine-tuning. In the shared file rather than the GPU one
# because both environments need it: the host trains the LoRA adapters and the
# Mac applies them, folding the deltas into the base weights before indexing.
# `accelerate` comes with it — peft imports it — so it is pinned here too rather
# than floating to whatever the resolver picks on each machine.
peft==0.14.0
accelerate==1.1.1

# Retrieval
faiss-cpu==1.9.0.post1
rank-bm25==0.2.2

# Neither environment installs torch-geometric any more: the only importer was
# the archived baseline's GCN label refiner, and ticket 14 excluded it (the
# vocabulary has no hierarchy to convolve over).

# Analysis and reporting
matplotlib==3.9.3
openpyxl==3.1.5
tqdm==4.67.1
